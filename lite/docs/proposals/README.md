# Proposals (RFCs)

Pre-decision design documents: `NNN-kebab-case-title.md`. A proposal explores a problem and
alternatives *before* commitment; once a direction is chosen, the decision itself is recorded
as an ADR in `docs/decisions/` and the proposal is referenced from it. `/plan` accepts a
proposal number as input to break an accepted proposal into sprints.
